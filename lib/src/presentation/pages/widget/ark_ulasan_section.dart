import 'package:flutter/material.dart';

class ArkUlasanSection extends StatelessWidget {
  const ArkUlasanSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black,
    );
  }
}
