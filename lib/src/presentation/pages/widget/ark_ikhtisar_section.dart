import 'package:flutter/material.dart';

class ArkIkhtisarSection extends StatelessWidget {
  const ArkIkhtisarSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      height: 250,
    );
  }
}
